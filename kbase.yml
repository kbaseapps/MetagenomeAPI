module-name:
    MetagenomeAPI

module-description:
    A KBase service for metagenomic data. Used for caching, search and data retrieval.

service-language:
    python

module-version:
    2.2.0

owners:
    [slebras]

service-config:
    dynamic-service: true

